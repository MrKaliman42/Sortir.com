<!DOCTYPE html>

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <meta charset="UTF-8">
    <title>{% block title %}Sortir.com{% endblock %}</title>
    {% block stylesheets %}{% endblock %}
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
          integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css2?family=Balsamiq+Sans&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('css/app.css') }}">
    <link rel="icon" href="{{ asset('img/logo.jpg') }}">
</head>
<body>


<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="col-lg-12">
            <div class="navbar-header">
                <a class="navbar-brand" href="{{ path ('home') }}">Sortie.com</a>
            </div>
            <div class="">
                <ul class="nav navbar pull-right">
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item active mr-auto">
                            <a class="nav-link" href="{{ path('register') }}">Particpant <span
                                        class="sr-only">(current)</span></a>
                        </li>

                        <li class="nav-item active mr-auto">
                            <a class="nav-link" href="{{ path('city') }}">Villes <span class="sr-only">(current)</span></a>
                        </li>
                        <li class="nav-item active mr-auto">
                            <a class="nav-link" href="{{ path('campus') }}">Campus <span
                                        class="sr-only">(current)</span></a>
                        </li>
                    {% endif %}
                    {% if is_granted('ROLE_USER') %}
                        <li class="nav-item active mr-auto">
                            <a href="{{ path('home') }}"> Accueil <span class="sr-only">(current)</span></a>
                        </li>
                        <li class="nav-item active mr-auto">
                            <a href="{{ path('monProfil') }}"> Mon profil <span class="sr-only">(current)</span></a>
                        </li>

                        <li class="nav-item active mr-auto">
                            <a href="{{ path('app_logout') }}"> Se deconnecter <span
                                        class="sr-only">(current)</span></a>
                        </li>
                    {% else %}
                        <li class="nav-item active mr-auto">
                            <a href="{{ path('app_login') }}"> Se connecter <span class="sr-only">(current)</span></a>
                        </li>
                    {% endif %}

                </ul>
            </div>

        </div>
    </nav>
</header>
<div class="d1"></div>
<div class="conteneur">
    <div class="d2"></div>
    <main>
        {% block main %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </main>

    <footer>

    </footer>
</div>
</body>
</html>
